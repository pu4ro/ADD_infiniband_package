# ==============================================================================
# Lustre 초기화 설정 파일 (더 이상 사용되지 않음)
# ==============================================================================
# 이 서비스는 이제 /etc/fstab을 사용하여 Lustre를 마운트합니다.
#
# /etc/fstab 예제:
# 192.168.1.10@o2ib:/lustre /mnt/lustre lustre defaults,_netdev 0 0
#
# LNet 설정은 /etc/lustre/lnetctl.conf 파일을 사용합니다.
# ==============================================================================
