# ==============================================================================
# .env 설정 파일 예제
# ==============================================================================
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env
# Makefile과 스크립트에서 사용할 환경 변수를 설정합니다.
# ==============================================================================

# 설치하려는 대상 서버의 커널 버전을 지정합니다.
# 예: TARGET_KERNEL := 6.2.0-37-generic
TARGET_KERNEL := 5.15.0-25-generic

# .deb 패키지들이 저장될 디렉토리 경로입니다.
# 상대 경로(debs) 또는 절대 경로(/root/debs) 모두 사용 가능합니다.
DEBS_DIR := debs

# 최종적으로 생성될 오프라인 배포용 압축 파일의 이름입니다.
ARCHIVE_NAME := offline_kit.tar.gz

# OFED 설치 파일이 포함된 디렉토리 경로를 지정합니다.
# 비워두면 스크립트가 'MLNX_OFED_LINUX-*.tgz' 패턴으로 자동 탐지합니다.
# 예: OFED_DIR := MLNX_OFED_LINUX-5.8-1.0.1.1-ubuntu22.04-x86_64
OFED_DIR :=